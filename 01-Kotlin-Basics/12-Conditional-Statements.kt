fun main() {
    val standardFinal = 75
    val kotlinFinal = 75


    if(kotlinFinal > standardFinal) { // if
        println("Raihan is a Kotlin Master")
    } else if (kotlinFinal == standardFinal) { // Else If
        println("You made it and you're right at the standard")
    } else { // Else
        println("Raihan is a Newbie")
    }
}